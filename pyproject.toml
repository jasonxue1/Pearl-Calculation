[project]
name = "Pearl-Calculator"
version = "0.1.0"
description = "A pearl calculator"
requires-python = "==3.12.11"
dependencies = [
    "numpy<2",
    "rich>=14.3.3",
    "torch<2.3",
    "tqdm>=4.67.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]
