[project]
name = "Pearl-Calculator"
version = "0.1.0"
description = "A pearl calculator"
requires-python = "==3.13.12"
dependencies = [
    "numpy>=2.4.2",
    "tqdm>=4.67.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]
